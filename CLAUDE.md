# CLAUDE.md

이 파일은 AI 어시스턴트(Claude 등)가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

- **저장소명:** test1
- **소유자:** obaltop-ai
- **상태:** 초기화 단계 (프로젝트 구조 미설정)
- **생성일:** 2026-02-18

이 저장소는 새로 초기화된 상태이며, 아직 본격적인 코드나 프로젝트 구조가 설정되지 않았습니다.

## 저장소 구조

```
/
├── .gitkeep          # 빈 디렉토리 유지용 파일
└── CLAUDE.md         # AI 어시스턴트 가이드 (이 파일)
```

## 개발 환경

- **버전 관리:** Git
- **원격 저장소:** origin (GitHub 호환)
- **기본 브랜치:** main

### 브랜치 전략

- `main` — 메인 브랜치 (원격)
- `master` — 로컬 기본 브랜치
- `claude/*` — AI 어시스턴트 작업용 브랜치

## 빌드 및 실행

현재 빌드 시스템, 패키지 매니저, 테스트 프레임워크가 설정되어 있지 않습니다.
프로젝트가 구성되면 이 섹션을 업데이트해 주세요.

### 빌드 명령어

```bash
# 아직 설정되지 않음
```

### 테스트 명령어

```bash
# 아직 설정되지 않음
```

### 린트/포맷팅

```bash
# 아직 설정되지 않음
```

## 코드 컨벤션

프로젝트에 코드가 추가되면 다음 항목을 정의해 주세요:

- 사용 언어 및 프레임워크
- 네이밍 규칙 (변수, 함수, 파일명 등)
- 디렉토리 구조 규칙
- 코드 스타일 (들여쓰기, 따옴표 등)
- 커밋 메시지 형식

## AI 어시스턴트 작업 시 주의사항

1. 변경 전 반드시 기존 코드를 먼저 읽고 이해할 것
2. 불필요한 파일 생성을 지양하고, 기존 파일 수정을 우선할 것
3. 보안 취약점(인젝션, XSS 등)이 발생하지 않도록 주의할 것
4. 과도한 엔지니어링을 피하고, 요청된 범위 내에서만 작업할 것
5. 커밋 메시지는 명확하고 간결하게 작성할 것
